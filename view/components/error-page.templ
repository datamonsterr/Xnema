package components

templ ErrorPage(message string) {
	<div class="flex flex-col items-center justify-center h-screen">
		<div class="text-3xl font-bold text-red-500">{ message }</div>
		<a href="/" class="mt-5 text-lg text-blue-500">Go back to home</a>
	</div>
}
